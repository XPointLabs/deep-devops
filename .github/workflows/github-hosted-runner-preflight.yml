name: github-hosted-runner-preflight

on:
  workflow_dispatch:
  push:
    branches: [release-candidate/prod-20260909]
    paths:
      - .github/workflows/github-hosted-runner-preflight.yml

permissions:
  contents: read

jobs:
  linux-docker-x64:
    name: Linux x64 Docker multi-architecture host
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Verify runner route and base commands
        shell: bash
        run: |
          set -euo pipefail
          test "${RUNNER_OS}" = Linux
          test "${RUNNER_ARCH}" = X64
          test "$(uname -m)" = x86_64
          for command in bash git jq base64 curl tar unzip pwsh; do
            command -v "${command}" >/dev/null
          done
          available_kib=$(df -Pk "${RUNNER_TEMP}" | awk 'NR == 2 { print $4 }')
          test "${available_kib}" -ge 10485760

      - name: Verify Docker daemon access
        shell: bash
        run: |
          set -euo pipefail
          docker version
          docker info

      - uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - uses: docker/setup-buildx-action@v3

      - name: Verify Buildx and Linux ARM64 execution
        shell: bash
        run: |
          set -euo pipefail
          docker buildx inspect --bootstrap
          test "$(docker run --rm --platform linux/arm64 alpine:3.22 uname -m)" = aarch64

  linux-android-x64:
    name: Linux x64 managed and Android build host
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Verify runner route and base commands
        shell: bash
        run: |
          set -euo pipefail
          test "${RUNNER_OS}" = Linux
          test "${RUNNER_ARCH}" = X64
          test "$(uname -m)" = x86_64
          for command in bash git jq base64 curl tar unzip pwsh; do
            command -v "${command}" >/dev/null
          done
          available_kib=$(df -Pk "${RUNNER_TEMP}" | awk 'NR == 2 { print $4 }')
          test "${available_kib}" -ge 10485760

      - uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}/dotnet
        with:
          dotnet-version: 10.0.301

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - uses: android-actions/setup-android@v3
        env:
          ANDROID_HOME: ${{ runner.temp }}/android-sdk
          ANDROID_SDK_ROOT: ${{ runner.temp }}/android-sdk

      - name: Verify managed and Android build tools
        shell: bash
        run: |
          set -euo pipefail
          test "$(dotnet --version)" = 10.0.301
          node --version
          java -version
          sdkmanager --version

  windows-x64:
    name: Windows x64 client and native cross-build host
    runs-on: windows-2022
    timeout-minutes: 30
    steps:
      - name: Verify runner route and free space
        shell: powershell
        run: |
          if ($env:RUNNER_OS -cne 'Windows' -or $env:RUNNER_ARCH -cne 'X64') {
            throw "Unexpected runner route: $env:RUNNER_OS/$env:RUNNER_ARCH"
          }
          if ([Runtime.InteropServices.RuntimeInformation]::OSArchitecture -ne
              [Runtime.InteropServices.Architecture]::X64) {
            throw 'The Windows runner host is not x64.'
          }
          $drive = Get-PSDrive -Name ([IO.Path]::GetPathRoot($env:RUNNER_TEMP).TrimEnd(':\'))
          if ($drive.Free -lt 20GB) { throw 'The Windows runner needs at least 20 GiB free.' }

      - uses: actions/setup-dotnet@v4
        env:
          DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}\dotnet
        with:
          dotnet-version: 10.0.301

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Verify pinned Visual Studio cross-build components
        shell: powershell
        run: |
          $vswhere = Join-Path ${env:ProgramFiles(x86)} 'Microsoft Visual Studio\Installer\vswhere.exe'
          if (-not (Test-Path -LiteralPath $vswhere -PathType Leaf)) {
            throw 'Visual Studio Installer/vswhere is missing.'
          }
          $components = @(
            'Microsoft.VisualStudio.Component.VC.Tools.x86.x64',
            'Microsoft.VisualStudio.Component.VC.Tools.ARM64',
            'Microsoft.VisualStudio.Component.VC.CMake.Project'
          )
          foreach ($component in $components) {
            $installation = (& $vswhere -latest -products '*' -requires $component -property installationPath | Out-String).Trim()
            if ([string]::IsNullOrWhiteSpace($installation)) {
              throw "Missing Visual Studio component: $component"
            }
          }
          $sdk = Join-Path ${env:ProgramFiles(x86)} 'Windows Kits\10\Include\10.0.26100.0'
          if (-not (Test-Path -LiteralPath $sdk -PathType Container)) {
            throw 'Pinned Windows SDK 10.0.26100.0 is missing.'
          }
          if ((dotnet --version) -cne '10.0.301') {
            throw 'Pinned .NET SDK 10.0.301 is unavailable.'
          }
          git --version
          node --version
          if (-not (Get-Command pwsh.exe -ErrorAction SilentlyContinue)) {
            throw 'PowerShell 7 (pwsh.exe) is required by the repository workflows.'
          }
          if (-not (Get-Command rustup.exe -ErrorAction SilentlyContinue) -or
              -not (Get-Command cargo.exe -ErrorAction SilentlyContinue)) {
            throw 'rustup.exe and cargo.exe must be available to the runner service account.'
          }
          $probeRoot = Join-Path $env:RUNNER_TEMP "deep-symlink-probe-$([Guid]::NewGuid().ToString('N'))"
          $target = Join-Path $probeRoot 'target'
          $link = Join-Path $probeRoot 'link'
          [IO.Directory]::CreateDirectory($target) | Out-Null
          try {
            [IO.Directory]::CreateSymbolicLink($link, $target) | Out-Null
          }
          catch {
            throw 'The GitHub-hosted runner must be allowed to create symbolic links for the filesystem security tests.'
          }
          finally {
            if ([IO.Directory]::Exists($link)) { [IO.Directory]::Delete($link) }
            if ([IO.Directory]::Exists($target)) { [IO.Directory]::Delete($target) }
            if ([IO.Directory]::Exists($probeRoot)) { [IO.Directory]::Delete($probeRoot) }
          }
